# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build
node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment & secrets
.env
.env.*
!.env.example

# Database na raiz (scripts SQL locais — não versionar)
/database/

# Sistema legado (referência local, contém .env e builds)
SIstema-Antigo/

# Lovable (metadados da plataforma de desenvolvimento)
.lovable/

# Backend (código versionado; artefatos locais abaixo)
backend/.env
backend/node_modules
backend/.next
backend/uploads

# Extensão do navegador (artefatos de build)
extension/dist/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Lock file alternativo (projeto usa bun)
package-lock.json

# Deploy VPS (secrets locais)
scripts/deploy.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
